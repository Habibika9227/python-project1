

# ✅ Exercise 1 — Rectangle area

# Create a function that:

# takes width and height

# returns the area

# In main(), call this function three times with different values.

# Add all three returned areas together and print the total.



            #,EXERCISE 1

# def area(width,height):
#     return width*height


# def main():
#    area1=area(20,89)
#    area2=area(30,89)
#    area3=area(40,90)
#    total=area1+area2+area3
#    print(f"the total area is {total} square meters")
# main()


     #.EXERCISE 2
# ✅ Exercise 2 — Convert minutes to seconds
# Create a function that:
# takes minutes
# returns the number of seconds
# In main(), ask the user for two different minute values
# Convert both using your function
# Add them together and print the result  

# def receive_min(minutes):
#     return minutes*60
# def main():
#     minute1=int(input("select a minute: "))
#     minute2=int(input("select another minute: "))
#     sec1=receive_min(minute1)
#     sec2=receive_min(minute2)
#     total=sec1+sec2
#     print(f"the total secs are {total}")
# main() 


# ✅ Exercise 3 — Temperature converter
# Create a function that:
# takes Celsius temperature
# returns the equivalent in Fahrenheit
# (You must look up the formula)
# In main(), call this function for 3 different Celsius values and print each one.

# def temperature_convertor(celsius):
#     return celsius*32
# def main():
#     fahr1=temperature_convertor(20)
#     fahr2=temperature_convertor(36)
#     total=fahr1+fahr2
#     print(f"celsius -> fahrenheit: {total}")

# main()


# ✅ Exercise 4 — Discount calculator

# Create a function that:
# takes price and discount percentage
# returns the final price after discount
# In main(), calculate the final cost of:
# a phone
# a laptop
# a watch
# Add them and print the total cost.

# def price_after_discount(price):
#     return price*0.10
# def main():
#     cost_of_phone=price_after_discount(500)
#     cost_of_laptop=price_after_discount(2345)
#     cost_of_watch=price_after_discount(56)
#     total_cost=cost_of_phone+cost_of_laptop+cost_of_watch
#     print(f"the final cost is: {total_cost}")


# main()    




# Exercise 5 — Maximum of two numbers
# Create a function that:
# takes two numbers
# returns the bigger one
# In main(), call it 4 times with different pairs and print the returned value each time.
def two_number(bigger,smaller):
     
    return bigger
    
def main():
    call1=two_number(13,11)
    # call2=two_number(15,10)
    # call3=two_number(18,20)
    print(call1)


main()
